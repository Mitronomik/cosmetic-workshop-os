# C4-III pre-partial-verification snapshots

These files are byte-identical Git blobs from merged main
`81e8193596709b0c16d0ecad598458b3ea95fd9c`, immediately after PR #189 merged and before the C4-III partial-verification checkpoint rewrote the active lifecycle status.

They are historical, non-normative snapshots. Current authority lives in the active files at repository root / `docs/` / `state/`.

The snapshot exists because `docs/AGENTS.md` requires preserving the full prior copy before rewriting active lifecycle documentation. It preserves the exact `C4-III — AUTHORIZED NEXT — NOT IMPLEMENTED` wording that was true before the external exact-head verification result was recorded.
