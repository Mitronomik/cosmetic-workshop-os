# backend/AGENTS.md

Backend rules: domain logic in services, migrations for DB changes, Decimal for calculations, transactional production, ImportDraft for imports, AuditLog for important actions, no sensitive notes in logs, tests required.
