import { PurchaseSuggestionFilters, PurchaseSuggestionLike, PurchaseSuggestionsFeedbackLifecycle, GenerationResult } from './purchase-suggestions-feedback';
export type PurchaseSuggestionRuntimeDeps<T extends PurchaseSuggestionLike>={read:(filters:PurchaseSuggestionFilters)=>Promise<T[]>;regenerate:()=>Promise<GenerationResult>;create:(payload:any)=>Promise<T>;update:(id:number,payload:any)=>Promise<T>;markPurchased:(id:number)=>Promise<T>;dismiss:(id:number)=>Promise<T>;render?:()=>void;announce?:(text:string,kind:'polite'|'assertive')=>void;focus?:(key:string|null)=>void};
export function createPurchaseSuggestionsRuntime<T extends PurchaseSuggestionLike>(lifecycle:PurchaseSuggestionsFeedbackLifecycle<T>,deps:PurchaseSuggestionRuntimeDeps<T>){function present(r:any){if(r.present){deps.render?.(); if(r.announcement!=='none')deps.announce?.(lifecycle.state.feedback.success||lifecycle.state.feedback.mutationError||lifecycle.state.feedback.refreshWarning,r.announcement); deps.focus?.(r.focusKey);}}
function reconcile(){const s=lifecycle.startReconciliationRead(lifecycle.displayedFilters()); if(!s.accepted)return; deps.read(s.filters).then(items=>present(lifecycle.finishReadSuccess(s.requestId,s.routeGeneration,s.filters,items))).catch(()=>present(lifecycle.finishReadFailure(s.requestId,s.routeGeneration,s.filters)));}
function read(kind:any,filters:PurchaseSuggestionFilters){const s=lifecycle.startRead(kind,filters); if(!s.accepted)return s; deps.render?.(); deps.read(s.filters).then(items=>present(lifecycle.finishReadSuccess(s.requestId,s.routeGeneration,s.filters,items))).catch(()=>present(lifecycle.finishReadFailure(s.requestId,s.routeGeneration,s.filters))); return s;}
function mutation(kind:any,promise:Promise<T>,owner:any){promise.then(dto=>{const r=lifecycle.applyResponse(owner.requestId,owner.routeGeneration,kind,owner.owner.suggestionId,dto); if(!r.accepted){const d=lifecycle.settleDetachedResponse(owner.requestId,kind,owner.owner.suggestionId,dto); if(d.settledDetached)reconcile(); return;} present(r);}).catch(()=>{const r=lifecycle.finishMutationFailure(owner.requestId,owner.routeGeneration,kind,owner.owner.suggestionId); if(!r.accepted){const d=lifecycle.settleDetachedFailure(owner.requestId,kind,owner.owner.suggestionId); if(d.settledDetached)reconcile(); return;} present(r);});}
return{read,reconcile,regenerate(){const s=lifecycle.startMutation('regenerate'); if(!s.accepted)return s; deps.render?.(); deps.regenerate().then(result=>{const r=lifecycle.finishRegenerationSuccess(s.requestId,s.routeGeneration,result); if(!r.accepted){const d=lifecycle.settleDetachedRegenerationSuccess(s.requestId,result); if(d.settledDetached)reconcile(); return;} deps.render?.(); const g=lifecycle.startRead('regeneration-refresh',lifecycle.displayedFilters(),s.routeGeneration); if(!g.accepted)return; deps.read(g.filters).then(items=>present(lifecycle.finishReadSuccess(g.requestId,g.routeGeneration,g.filters,items))).catch(()=>present(lifecycle.finishReadFailure(g.requestId,g.routeGeneration,g.filters)));}).catch(()=>{const r=lifecycle.finishMutationFailure(s.requestId,s.routeGeneration,'regenerate',null); if(!r.accepted){const d=lifecycle.settleDetachedFailure(s.requestId,'regenerate',null); if(d.settledDetached)reconcile(); return;} present(r);}); return s;},create(payload:any){const s=lifecycle.startMutation('create-manual',undefined,null,{expectedItemType:payload.item_type,expectedItemId:payload.item_id,previousFocusKey:'manual-purchase-form'}); if(s.accepted)mutation('create-manual',deps.create(payload),s); return s;},update(id:number,payload:any){const s=lifecycle.startMutation('update',undefined,id,{previousFocusKey:`purchase-suggestion-${id}-edit`}); if(s.accepted)mutation('update',deps.update(id,payload),s); return s;},markPurchased(id:number){const s=lifecycle.startMutation('mark-purchased',undefined,id,{previousFocusKey:`purchase-suggestion-${id}`}); if(s.accepted)mutation('mark-purchased',deps.markPurchased(id),s); return s;},dismiss(id:number){const s=lifecycle.startMutation('dismiss',undefined,id,{previousFocusKey:`purchase-suggestion-${id}`}); if(s.accepted)mutation('dismiss',deps.dismiss(id),s); return s;}};}
