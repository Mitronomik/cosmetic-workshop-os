# UI/UX Contract

The app must not feel like a technical admin panel. User-facing labels are Russian. Empty states explain next action. Dangerous actions require confirmation. Raw stack traces are never shown. Complex fields have hints. Dashboard explains today's work. Client card is a working center.
